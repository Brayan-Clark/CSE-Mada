---
import ThemeToggle from './ThemeToggle.astro';

const base = '/CSE-Mada';
const links = [
  { name: 'Accueil', href: `${base}/` },
  { name: 'Matériel', href: `${base}/materiel` },
  { name: 'Services', href: `${base}/services` },
  { name: 'Contact', href: `${base}/contact` },
];
---

<header class="sticky top-0 z-50 w-full border-b border-border/40 bg-background/95 backdrop-blur supports-[backdrop-filter]:bg-background/60">
  <div class="container w-full mx-auto px-4 sm:px-6 lg:px-8 flex h-16 items-center justify-between">
    <a href="/CSE-Mada/" class="flex items-center space-x-2 font-bold text-lg">
      <img src="/CSE-Mada/images/CSE-logo.jpg" alt="Logo" class="w-16" />
      <span class="text-green-700">Camping Store & Event</span>
    </a>
    
    <nav class="hidden md:flex items-center space-x-6 justify-center">
      {links.map(link => (
        <a 
          href={link.href} 
          class="text-sm font-medium hover:text-primary transition-colors"
        >
          {link.name}
        </a>
      ))}
      <ThemeToggle />
    </nav>
    
    <!-- Mobile menu button -->
    <button class="md:hidden p-2" id="mobile-menu-button">
      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
        <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
      </svg>
    </button>
  </div>
  
  <!-- Mobile menu -->
  <div class="md:hidden hidden border-t border-border/40 bg-background/95 backdrop-blur supports-[backdrop-filter]:bg-background/60" id="mobile-menu">
    <div class="container py-4 space-y-4">
      {links.map(link => (
        <a 
          href={link.href} 
          class="block py-2 text-foreground hover:text-primary transition-colors"
        >
          {link.name}
        </a>
      ))}
      <div class="pt-2 flex justify-center">
        <ThemeToggle />
      </div>
    </div>
  </div>
  
  <script>
    // Gestion du menu mobile
    document.getElementById('mobile-menu-button')?.addEventListener('click', () => {
      const menu = document.getElementById('mobile-menu');
      menu?.classList.toggle('hidden');
    });
</script>
</header>
